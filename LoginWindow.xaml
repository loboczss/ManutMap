<Window x:Class="ManutMap.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Matrícula:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="MatriculaBox" Height="25"/>
        <TextBlock Grid.Row="2" x:Name="StatusText" Foreground="Red" Margin="0,5,0,5"/>
        <Button Grid.Row="3" x:Name="LoginButton" Content="Entrar" Width="100" Height="30" HorizontalAlignment="Right" Click="LoginButton_Click"/>
    </Grid>
</Window>
