<Window x:Class="ManutMap.DatalogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Buscar Datalog" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Início:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="DpInicio" SelectedDate="{x:Static sys:DateTime.Today}" Margin="0,0,10,0"/>
            <TextBlock Text="Fim:" VerticalAlignment="Center" Margin="20,0,10,0"/>
            <DatePicker x:Name="DpFim" SelectedDate="{x:Static sys:DateTime.Today}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <ComboBox x:Name="CmbFiltro" Width="120" Margin="0,0,10,0">
                <ComboBoxItem Content="INSTALAÇÃO"/>
                <ComboBoxItem Content="OS"/>
                <ComboBoxItem Content="IDSIGFI"/>
                <ComboBoxItem Content="ROTA"/>
            </ComboBox>
            <TextBox x:Name="TxtBusca" Width="200" Margin="0,0,10,0"/>
            <ComboBox x:Name="CmbRegional" Width="100" Margin="0,0,10,0">
                <ComboBoxItem Content="TODAS"/>
                <ComboBoxItem Content="AC"/>
                <ComboBoxItem Content="MT"/>
            </ComboBox>
            <Button x:Name="BtnBuscar" Content="BUSCAR" Click="BtnBuscar_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="GridOs" AutoGenerateColumns="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="OS" Binding="{Binding NumOS}" Width="*"/>
                <DataGridTextColumn Header="IDSIGFI" Binding="{Binding IdSigfi}" Width="*"/>
                <DataGridTextColumn Header="ROTA" Binding="{Binding Rota}" Width="*"/>
                <DataGridTextColumn Header="DATA" Binding="{Binding Data}" Width="*"/>
                <DataGridTextColumn Header="STATUS" Binding="{Binding Status}" Width="*"/>
                <DataGridTemplateColumn Header="PASTA" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="ABRIR PASTA"
                                    Command="{Binding DataContext.CmdAbrir, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding FolderUrl}"
                                    Visibility="{Binding TemDatalog, Converter={StaticResource BoolToVis}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" x:Name="TxtResumo"/>
    </Grid>
</Window>
